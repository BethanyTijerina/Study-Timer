from timer import PomodoroTimer

class Task:
    def Task(self,task_name,task_amount):# AM: defines the task name and when object is made amount can be set to zero
        self.task_name=task_name
        self.task_amount=self.task_amount
    def new_task(self,name):# AM: creates a new task and keeps track of task amounts
        self.task_name=name
        self.task_amount+=1
        


